"use strict";
//how to Write a program to convert the temperature from Celsius to Fahrenheit and vice verse using function.
function ctof(_temp) {
    var temp = 39;
    var temf = _temp * (9 / 5) + 32;
    console.log(temf);
}
